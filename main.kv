#:include pipe.kv

FloatLayout:
    Background:
        id: background
        canvas.before:
            Rectangle:
                size: self.size
                pos: self.pos[0], self.pos[1]
                texture: self.sky_texture
    Image:
        id: gif
        source: 'to_left_frames.zip'
        pos: self.pos
        size_hint: None, None
        size: root.size
        allow_stretch: True
        keep_ratio: False
        size_hint_y: 1
    Image:
        id: logo
        source: 'LOGO.png'
        size_hint_y: None
        pos_hint: {'top': 0.8}
        size: 70,70

    Label:
        id: score
        size_hint_y: None
        pos_hint: {'top': 1}
        text: "0"
        font_size: 40
    Button:
        text: "Start game"
        font_size: 60
        pos_hint: {'top': 0.75}
        background_normal: "transparent.png"
        background_down: "transparent.png"
        id: start_button
        on_release:
            self.disabled = True
            self.opacity = 0
            root.remove_widget(root.ids.logo)
            app.start_game()
    Bird:
        source: "bird1.png"
        size_hint: None, None
        size: 46, 34
        pos: (root.width-50)/2.0, (root.height)/2.0-100
        id: bird
